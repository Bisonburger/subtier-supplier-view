{
  "name": "ssv-e2e-test",
  "version": "0.0.1",
  "private": true,
  "description": "E2E testing for the SSV project",
  "repository": {
    "type": "svn",
    "url": "https://rayforge.app.ray.com/svn/rmsitssv/trunk/"
  },
  "license": "MIT",
  "devDependencies": {
    "angular": "1.5.8",
    "angular-mocks": "1.5.8",
    "protractor": "^5",
    "webdriver-manager": "^11",
    "jasmine-reporters": "^2.2.0",
    "protractor-html-reporter": "^1.3.1"
  },
  "scripts": {
    "test": "protractor protractor-config.js",
    "webdriver-update": "webdriver-manager --proxy http://tus-proxy.ext.ray.com:80 --ignore_ssl update",
    "webdriver-update-ie": "webdriver-manager --proxy http://tus-proxy.ext.ray.com:80 --ignore_ssl update --ie --versions.ie=2.53.1"
  }
}
